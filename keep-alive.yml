name: Keep Strides Awake
on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes
  workflow_dispatch:           # manual trigger button
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Strides health endpoint
        run: curl -sSf -m 30 "https://koenig-strides-wpn647aofdopamvcypcosu.streamlit.app/?ping=1" > /dev/null
