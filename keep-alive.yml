name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "*/20 * * * *"
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -L --fail https://YOUR-APP-NAME.streamlit.app
