name: Keep Streamlit Alive

on:
  schedule:
    - cron: '*/5 * * * *'   # every 5 minutes
  workflow_dispatch:         # manual trigger too

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Strides
        run: curl -fsS "https://YOUR-STRIDES-URL/?ping=1" -o /dev/null
